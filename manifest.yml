---
declared-services:
  tone-analyzer-service-experimental:
    label: tone_analyzer
    plan: experimental
applications:
- name: tone-analyzer-demo
  command: node app.js
  path: .
  memory: 512MB
  services:
  - tone-analyzer-service-experimental
