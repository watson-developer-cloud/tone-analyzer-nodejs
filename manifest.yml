---
declared-services:
  tone-analyzer-service-experimental:
    label: tone_analyzer
    plan: experimental
applications:
- services:
  - Tone Analyzer-tj
  name: tone-analyzer-demo
  command: node app.js
  path: .
  memory: 512MB
  env:
    NODE_ENV: production
